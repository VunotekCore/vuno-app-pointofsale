; Requerido para que electron-builder empaquete correctamente todas las
; dependencias transitivas (como 'ms', 'debug') dentro del .asar.
; Sin esto, pnpm usa symlinks que electron-builder no puede seguir,
; causando "Cannot find module 'ms'" en el AppImage/exe empaquetado.
shamefully-hoist=true
