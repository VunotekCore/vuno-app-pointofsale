# Para proyectos con electron-builder, pnpm requiere el linker "hoisted"
# porque electron-builder falla al seguir los symlinks (accesos directos).
# Esto crea una carpeta node_modules plana exactamente igual a npm,
# solucionando los errores de "Cannot find module 'X'".
node-linker=hoisted
